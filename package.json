{
  "name": "env-version-tracker",
  "version": "1.0.0",
  "description": "A CLI tool to track deployments version across multiple environments",
  "main": "cli/index.js",
  "bin": {
    "env-version-tracker": "./cli/index.js"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "husky install || true"
  },
  "keywords": [
    "cli",
    "version",
    "tracking",
    "deployment",
    "environment",
    "git",
    "mongodb",
    "versioning"
  ],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=14.0.0"
  },
  "files": [
    "cli/**/*"
  ],
  "dependencies": {
    "commander": "^14.0.2",
    "mongodb": "^7.0.0",
    "inquirer": "^9.2.0"
  },
  "devDependencies": {
    "husky": "^8.0.3"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Fy-Rakotondrabe/env-version-tracker"
  }
}
